# BuyLow Telegram Bot (Render-ready)

## Deploy on Render

1. Push this repo to GitHub
2. Create **New → Background Worker** on Render
3. Select the repo
4. Set environment variable **BOT_TOKEN**
5. Deploy

Bot runs via long polling (no webhook needed).
